#!/usr/bin/env python
from distutils.core import setup

setup(
    name='mplisp',
    version='0.2',
    packages=[
        'mplisp',
    ],
    scripts=[
        "scripts/mplisp"
    ],
    install_requires=[
    ]
)
